{
  "name": "controllo-invitato",
  "author": "Wedding-Francis",
  "version": "1.0.0",
  "description": "Lambda per controllare se una persona e' invitata",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "postbuild": "cp package.json yarn.lock dist && cd dist && yarn --production --frozen-lockfile --mutex network",
    "lint": "eslint '*/**/*.{js,ts,tsx}' --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "@types/aws-lambda": "^8.10.141",
    "eslint": "^9.7.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-vue": "^9.27.0",
    "aws-sdk": "^2.1659.0"
  },
  "dependencies": {}
}